# Вклад

1. Новые записи — только со стабильным `id` и полем `sources`.
2. Изменения схемы — через PR с обновлением `datapackage.json`.
3. Склонения высокочастотных имён — вручную, `review=false`.
4. Не копировать крупные дампы источников в `data/curated/`.
5. Обязательна запись в `CHANGELOG.md`.
